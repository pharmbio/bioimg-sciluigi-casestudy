---
- name: Install luigi (and sciluigi) via pip
  pip:
    name={{ item }}
  with_items:
    - luigi
    - tornado
    - sciluigi
  environment:
    PYENV_ROOT: "{{ home }}/.pyenv"
    PYENV_SHELL: "bash"
    PATH: "{{ home }}/.pyenv/shims:{{ home }}/.pyenv/bin:$PATH:/usr/bin:/bin:$PATH"

